<import name="anu-page-wrapper" src="../../../components/PageWrapper/index.ux"></import>
<template>
    <anu-page-wrapper>
        <div class="anu-block">
            <block for="(i767,item) in state.list">
                <div class="anu-item" onclick="dispatchEvent" data-click-uid="{{'e36_38_' + i767}}" data-beacon-uid="default"><text>{{item.name}}</text></div>
            </block>
        </div>
    </anu-page-wrapper>
</template>
<script>
import React from "../../../ReactQuick.js";

function P() {
    this.state = {
        list: 'fillStyle,arc,strokeStyle,textBaseline,rect,globalAlpha'.split(',').map(function(name) {
            return {
                url: '../../../../pages/apis/canvas' + "/" + name + "/index",
                name: name
            };
        })
    };
}

P = React.toClass(P, React.Component, {
    componentDidMount: function() {},
    gotoSome: function(url) {
        console.log('url', url);

        if (url) {
            React.api.navigateTo({
                url
            });
        }
    },
    render: function() {
        var h = React.createElement;
        return h("div", {
            class: "anu-block"
        }, this.state.list.map(function(item, i767) {
            return h("div", {
                className: "anu-item",
                onClick: this.gotoSome.bind(this, item.url),
                "data-click-uid": 'e36_38_' + i767,
                "data-beacon-uid": "default"
            }, h("text", null, item.name));
        }, this));;
    },
    classUid: "c1162"
}, {});
P = React.registerPage(P, "pages/apis/canvas/index");
export default P;
</script>
<style>
@import '../../../assets/global.css';
</style>